---
- hosts: all
  become: yes
  become_user: root
  roles:
  # - create_user
  # - set_up_ssh_for_user
    - install_dependencies

- hosts: control_plane
  become: yes
  become_user: root
  roles:
    - install_kubectl
    - initialize_cluster
    - install_Pod_network
    - get_join_command

- hosts: workers
  become: yes
  become_user: root
  roles:
    - join_workers_in_cluster
